from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='using simple usb camera for air quality measurements',
    author='Scott Pitz',
    license='MIT',
)
